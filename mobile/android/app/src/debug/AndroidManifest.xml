<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Local chat testing reaches the development API through adb reverse.
         This overlay is used only by debug builds; release stays HTTPS-only. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
